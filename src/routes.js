import React from 'react';
import {
  BrowserRouter as Router,
  Route,
} from 'react-router-dom'

const Routes = () => {
	return (
		<Router>
			<Route exact path="/">
			</Route>
		</Router>
	);
}

export default Routes;
